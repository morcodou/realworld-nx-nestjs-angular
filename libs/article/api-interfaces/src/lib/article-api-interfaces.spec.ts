import { articleApiInterfaces } from './article-api-interfaces';

describe('articleApiInterfaces', () => {
  it('should work', () => {
    expect(articleApiInterfaces()).toEqual('article-api-interfaces');
  });
});
